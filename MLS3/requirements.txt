# Core ML/DL libraries
tensorflow>=2.12.0
keras>=2.12.0
# TensorFlow 2.12 wheels require NumPy < 1.24; keep within tested range
numpy>=1.22.0,<1.24.0

# Data processing and visualization
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Performance profiling and monitoring
psutil>=5.9.0
memory-profiler>=0.60.0
line-profiler>=4.0.0

# Model optimization and quantization
tensorflow-model-optimization>=0.7.0

# Platform-specific deployment
onnx>=1.14.0
onnxruntime>=1.15.0

# Simulation and emulation (Track B) - Install separately if needed
# qemu-system-arm (system package, not pip)
# renode (system package, not pip)

# Hardware-specific libraries - Install separately if needed
# ARM Compute Library (install from source)
# CMSIS-NN (include headers for TFLite Micro)

# Jupyter and development tools
jupyter>=1.0.0
ipywidgets>=8.0.0
black>=23.0.0
flake8>=6.0.0

# Performance benchmarking - Use TensorFlow/ONNX Runtime built-in tools
# tflite-benchmark-model (use tensorflow-lite tools)
# onnxruntime-perf-test (use onnxruntime tools)

# Power and energy monitoring - System tools, install separately if needed
# powertop (Linux system package)
# turbostat (Linux system package)

# WebGPU simulation (Track B)
# Install via browser: Chrome/Edge with WebGPU support

# Additional utilities
scipy>=1.10.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pyyaml>=6.0